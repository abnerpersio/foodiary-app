import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingVertical: 8,
    paddingHorizontal: 24,
  },
  heading: {
    letterSpacing: -0.32,
  },
  form: {
    marginTop: 24,
    gap: 24,
  },
});
